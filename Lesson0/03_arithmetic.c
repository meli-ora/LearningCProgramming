// 260806
#include <stdio.h>

int main() {

    // + - * / % are add subtract multiply divide and modulus operators respectively
    // I know how those work

    // -- is decrement ++ is increment

    int x = 5;

    x = x + 2;
    x += 2; // same as above
    x -= 4;
    x *= 10;
    x /= 2;

    printf("%d\n", x);

    return 0;

}